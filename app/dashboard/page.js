import DashboardPageWrapper from "@/components/wrappers/DashboardPageWrapper";
import React from "react";

export const metadata = {
  title: "داشبورد مدیریت | فروشگاه اینترنتی امیران واچ",
  robots: {
    index: false,
    follow: false,
  },
};

const page = () => {
  return <DashboardPageWrapper />;
};

export default page;
